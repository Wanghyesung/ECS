---
name: unity-feature
description: "Unity 기능을 계획하고 구현합니다 — 서브시스템을 식별하고, 스킬을 로드하고, 코드를 작성하고, MCP를 통해 씬 요소를 설정합니다."
user-invocable: true
args: feature_description
---

# /unity-feature — 기능 구현

사용자가 설명한 기능을 계획하고 구현합니다: **$ARGUMENTS**

## 에이전트 라우팅

- 기본값: `unity-coder` 에이전트 사용(opus — 완전한 아키텍처적 추론)
- `$ARGUMENTS`에 `--quick`이 포함된 경우: `unity-coder-lite` 에이전트 사용(sonnet — 단순 추가 작업을 위한 더 빠른 처리)
- `$ARGUMENTS`에 `--no-test`가 포함된 경우: 4단계(통합 테스트 생성)를 건너뜁니다.
- 에이전트에 전달하기 전에 인자에서 `--quick`, `--no-test` 플래그를 제거합니다.

## 1단계: 계획

1. **프로젝트를 스캔**합니다 — `unity-scout`(haiku)로 `CLAUDE.md`를 읽고, 관련된 기존 스크립트/필드/최적화를 찾고, 어셈블리 구조를 매핑합니다. 이미 존재하는 필드 조합으로 표현 가능한 요구사항인지 먼저 확인합니다(예: 새 로직을 추가하기 전에 기존 카운터/플래그 조합으로 되는지부터 검토).

2. **기능을 분석**합니다 — 어떤 Unity 서브시스템이 관련되는지 식별합니다:
   - 입력 시스템? 물리? 애니메이션? UI? 오디오? 네트워킹?
   - 기존의 어떤 스크립트/시스템과 통합되는지?

3. **필요한 스크립트를 식별**합니다 — 새로 만들 스크립트와 수정할 기존 스크립트를 파악합니다.

4. **씬 변경 사항을 식별**합니다 — 어떤 GameObject, 컴포넌트, 씬 설정이 필요한지 파악합니다. 컴포넌트 존재 여부는 프리팹 에셋만 보지 말고, 필요하면 MCP로 실제 씬(.unity) 인스턴스를 확인합니다 — 씬 인스턴스가 프리팹과 구성이 다를 수 있습니다.

5. **Mermaid 다이어그램**으로 상태/흐름을 그려 제시합니다(문법은 `.claude/rules/architecture.md`의 "Mermaid 다이어그램 작성 규칙" 참고). 구현하기 전에 사용자 승인을 받습니다.

6. 구현하기 전에 사용자에게 **계획을 제시**합니다. 다음을 포함합니다:
   - 1단계에서 확인한 **관련 기존 코드를 구체적으로 인용**(파일:필드/메서드명) — 사용자가 계획만 보고도 실제 코드를 확인했는지 검증할 수 있어야 합니다
   - 생성/수정할 스크립트
   - MCP를 통한 씬 변경 사항
   - 기존 시스템에 대한 종속성
   - 예상 복잡도(간단 / 보통 / 복잡)

## 2단계: 구현

1. `unity-coder` 에이전트를 사용하여 **C# 코드를 작성**합니다:
   - `.claude/rules/`의 모든 규칙을 준수합니다.
   - 올바른 어셈블리 정의에 스크립트를 배치합니다.
   - 인스펙터 구성을 위해 `[SerializeField]`를 사용합니다.
   - 정리를 위해 `[Header]` 속성을 추가합니다.

2. MCP를 통해 **씬 요소를 설정**합니다:
   - `batch_execute`로 GameObject 생성
   - 컴포넌트 구성
   - 필요한 경우 물리 레이어 설정

3. `read_console`을 통해 컴파일 오류가 있는지 **콘솔을 확인**합니다.

## 3단계: 검증

1. `read_console`을 통해 콘솔 오류가 없는지 확인합니다.
2. 생성/수정된 내용을 요약합니다.
3. 기능을 테스트하는 방법을 설명합니다.
4. 필요한 수동 단계가 있으면 언급합니다(예: 인스펙터에서 참조 할당).
5. 더 좋은 기능이 있는지 확인합니다.

## 4단계: 통합 테스트 생성

(`--no-test`가 지정된 경우 이 단계를 건너뜁니다.)

1. **필요성 판단** — 이번 기능이 씬 내 여러 스크립트/오브젝트/물리/애니메이션의 상호작용을 포함하는지 확인합니다(예: 클릭 입력 → 배치 시스템 → UI 팝업 연동). 순수 로직/데이터 변경뿐이라면 이 단계를 생략하고 그 이유를 사용자에게 알립니다 — EditMode 테스트로 충분한 경우와 명확히 구분합니다.

2. **`unity-test-runner` 에이전트를 호출**해 다음을 계획으로 제시합니다:
   - 기능명(`<FeatureName>`, PascalCase) — 테스트 씬/스크립트/클래스 이름의 기준이 됩니다.
   - 테스트 씬 경로: `Assets/Tests/PlayMode/Scenes/<FeatureName>/<FeatureName>_IntegrationTest.unity`
   - 씬에 배치할 GameObject/컴포넌트/프리팹, 필요한 부트스트랩 매니저(예: `InputManager`, `ObjectPoolManager`)
   - 테스트 스크립트 경로: `Assets/Tests/PlayMode/<FeatureName>IntegrationTests.cs`
   - 검증할 어서션 목록(콘솔 에러 없음이 기본, 기능별 상태 검증 추가)

3. **사용자 확인 후 진행**:
   - UnityMCP 도구(`manage_scene`/`manage_gameobject`/`manage_components`/`manage_prefabs`, `batch_execute`로 묶어서)로 테스트 씬을 구성합니다 — `.claude/hooks/block-scene-edit.sh`가 `.unity` 파일 직접 편집을 막으므로 반드시 MCP를 사용해야 합니다.
   - `[UnityTest]` 통합 테스트 스크립트를 작성합니다(에이전트 정의의 "씬 기반 통합 테스트" 템플릿 참고). 클래스 전체를 `#if UNITY_INCLUDE_TESTS`로 감쌉니다 — 이 프로젝트는 asmdef 없이 암시적 Assembly-CSharp에 게임 코드가 컴파일되므로, 테스트 asmdef를 새로 만들지 않고 이 전처리기로 플레이어 빌드에서 제외합니다.
   - `run_tests`로 실행하고 `read_console`로 결과를 확인합니다.

4. 생성된 씬/테스트 스크립트 경로와 통과 여부를 요약합니다.

## 5단계: 자동 검증(선택 사항)

구현 후, 검증-수정 루프를 위해 `unity-verifier` 에이전트 실행을 제안합니다:
- 직렬화 안전성, 성능, Unity 관련 함정에 대해 변경된 모든 파일을 검토합니다.
- 안전한 문제(누락된 FormerlySerializedAs, CompareTag, 캐시되지 않은 GetComponent 등)를 자동으로 수정합니다.
- 최대 3회 반복하여 깨끗해질 때까지 재검증합니다.
- 사람의 판단이 필요한 남은 항목을 보고합니다.

다음과 같이 제안합니다: "변경 사항에 대한 검증 패스를 실행해 드릴까요?"
</content>
