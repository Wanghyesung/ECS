# 게임 기획서 (원페이지) — 2026-07-08 기준 현황 스냅샷

> 이 문서는 코드베이스 실사 결과를 반영한 **현재 진행 상황 기준** 기획서입니다. 스토리/세계관은 아직 확정되지 않아 컨셉 수준으로만 서술합니다.

## 1. 컨셉
- **장르**: 3D 우주 전투기 슈팅 (2D 종/횡스크롤 슈팅의 조작감과 규칙을 3D로 구현)
- **플랫폼/엔진**: Unity 6, URP
- **핵심 판타지**: 시점의 전투기를 조종해 소형 몬스터 함선과 거대 전함급 보스를 격추한다.

## 2. 핵심 플레이 루프 (현재 구현 상태)
1. 이동/회전으로 위치 선점 → 2. 조준(레이캐스트 크로스헤어) → 3. 사격(쿨타임 무기) → 4. 피격 연출(넉백/카메라 쉐이크) → **5. 적 처치 (미구현)**

**핵심 루프가 아직 닫히지 않음**: 총알이 몬스터에 맞아도 HP가 줄지 않고, 사망/파괴 처리가 없어 "적을 처치한다"는 슈팅 게임의 기본 목표가 현재는 성립하지 않음. 다음 스프린트 최우선 과제.

## 3. 시스템 구현 현황

| 시스템 | 상태 | 비고 |
|---|---|---|
| 플레이어 이동/조준/사격 | ✅ 동작 | Input System, Rigidbody 이동, 레이캐스트 조준 |
| 플레이어 FSM (Idle/Move/Dead) | 🟡 부분 | `eEntityState` enum만 존재, 상태 전이 로직 없음 |
| 몬스터 Behavior Tree | ✅ 동작 | Sequence/Selector/Parallel/ParallelWait/RandomSelect + SO 액션·조건 노드 20종+ |
| Blackboard | ✅ 동작 | 타겟/HP/상태이상/차지 등 공유 |
| 몬스터 상태이상(Stun/Poison/Burn 등) | ✅ 동작 | 비트마스크 기반 |
| 몬스터 HP 차감·사망 처리 | ❌ 미구현 | DoT 틱만 HP 차감, 직접 피격 데미지 없음. 처치 불가 |
| Object Pool (탄/이펙트) | ✅ 동작 | 자동 반납(AliveTime) |
| Object Pool (몬스터 스폰) | ❌ 미구현 | 몬스터 스포너/웨이브 없음, 씬에 수동 배치 |
| 이벤트 시스템 (Observer) | 🟡 부분 | 풀 반납·차지 완료 등 국소 콜백만 존재 |
| 점수/게임오버/GameManager | ❌ 미구현 | 관련 클래스 전무 |
| UI (체력바/타겟 정보) | 🟡 스텁 | `SilderUI`, `TargetManager` 로직 비어있음 |
| UI (메인메뉴/스코어/게임오버 화면) | ❌ 미구현 | — |
| 맵/배경 아트 | 🟡 임시 | 우주 테마와 맞지 않는 겨울 성 애셋(placeholder) 사용 중 |
| 빌드 세팅 | ❌ 미등록 | Build Settings에 씬 0개, 에디터 플레이만 가능 |

## 4. 적 구성 (현재 씬 기준)
- **일반 몬스터**: 모듈형 Sci-Fi 전투기 파츠 조합(BaseMonster0/1)
- **보스**: 다수 포탑을 가진 대형 전함형(BossMonster, USS-Virginia 개조) — 차지 후 조준 사격 패턴을 겨냥한 BT 구조

## 5. 알려진 이슈 (빌드/안정성)
- `Weapon.Awake()`가 비에디터 빌드에서 무조건 `Application.Quit()` 호출 → 빌드 시 즉시 종료됨
- `PlayerMovement.cs`에 `UnityEditor` 네임스페이스가 런타임 코드에 잔존 → 빌드 컴파일 실패 위험

## 6. 다음 우선순위 제안
1. **핵심 루프 완성**: 몬스터 `TakeDamage()`에 HP 차감 + 사망(파괴/이펙트/풀 반납) 로직 추가
2. **플레이어 FSM 완성**: Idle/Move/Dead 상태 전이 및 사망 처리 연결
3. **GameManager/점수/게임오버** 최소 골격 추가 (Observer 이벤트로 UI에 전달)
4. **HUD 체력바 연결** (`SilderUI` 로직 구현)
5. 빌드 차단 이슈 수정(Weapon Quit, Editor 네임스페이스) + Build Settings에 씬 등록
6. 몬스터 스포너 + Object Pool 연동, 맵 아트 우주 테마로 교체
