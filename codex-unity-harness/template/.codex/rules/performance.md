# Unity 성능 규칙

- Update·FixedUpdate·LateUpdate에서 반복 호출과 할당을 먼저 검토한다.
- `GetComponent`, `Find*`, `Camera.main` 참조는 생명주기에 맞춰 캐싱한다.
- 빈번히 생성되는 오브젝트는 프로젝트에 실제로 풀 구현이 있을 때 그 수명 규칙을 따른다.
- LINQ와 클로저의 핫 루프 할당을 확인하고 필요하면 명시적 반복문을 사용한다.
- 최적화는 측정값이나 명확한 반복 비용을 근거로 한다.
